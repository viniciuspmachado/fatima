

function Casados(props) {
    const casados = props.casados;
    //alert(casados);
    if (casados === '1') {
        return ('Sim') 
    } else
        return ('Não') 
    }

export default Casados;